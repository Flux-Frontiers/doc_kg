# doc_kg
DocKG builds a semantically-queryable knowledge graph from text-based input.

## MCP Quickstart

DocKG ships an MCP server via `dockg mcp` / `dockg-mcp`.

For setup and client configs (`.mcp.json`, `.vscode/mcp.json`, Claude Desktop), see:

- `docs/MCP.md`
